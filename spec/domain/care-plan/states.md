# Care-plan lifecycle

`draft → active → review_due → under_review → active|completed|ceased`.

Superseding creates a new version linked to the prior plan. `review_due` is computed from the plan's explicit review disposition. A plan does not complete simply because a billing item was invoiced or time elapsed.
