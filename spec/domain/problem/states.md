# Problem lifecycle

`proposed → active → inactive|resolved → active` and any non-final state → `entered_in_error`.

`proposed` is a clinician-visible suggestion awaiting acceptance, not part of the confirmed active summary. `inactive` means retained but not currently managed; `resolved` is a clinician assertion. Reactivation records a new event. Entered-in-error is privileged and reasoned.
